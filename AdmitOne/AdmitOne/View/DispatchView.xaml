<UserControl x:Class="AdmitOne.View.DispatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding ViewModel, RelativeSource={RelativeSource Self}}">
    <DockPanel>
        <Button DockPanel.Dock="Top" Command="{Binding GoBack}" HorizontalAlignment="Left">Back</Button>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <ListBox Grid.Column="0" ItemsSource="{Binding Techs}" />
            
            <ListBox Grid.Column="1" ItemsSource="{Binding Tickets}" />
        </Grid>
    </DockPanel>
</UserControl>
